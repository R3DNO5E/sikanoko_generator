name: Eslint
on:
  push:
    branches: [ '*' ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '21.x'
      - run: npm install
      - run: npm run lint